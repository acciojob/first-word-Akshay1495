function firstWord(str) {

    let str1 = str.split(" ");    
    return '"' + str1[0] + '"';     
} 
const s = prompt("Enter String:");
alert(firstWord(s));

// Do not change the code below

const s = prompt("Enter String:");
alert(firstWord(s));
